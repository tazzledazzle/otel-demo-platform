---
phase: 05-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt
  - api/src/main/kotlin/dev/otel/demo/api/routes/HealthRoutes.kt
  - api/src/main/kotlin/dev/otel/demo/api/Main.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/Main.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/Telemetry.kt
  - agent/agent/main.py
  - agent/agent/telemetry.py
  - agent/tests/
  - api/src/test/
  - worker/src/test/
  - docker-compose.yml
  - docker-compose.override.yml
  - docs/BROKEN_OBSERVABILITY.md
  - docs/TRACE_WALKTHROUGH.md
  - docs/PRODUCTION_CHECKLIST.md
  - docs/INCIDENTS.md
  - CONFIG.md
  - integration/README.md
autonomous: true

assumed_decisions:
  - "Phase 5 focuses on two primary incident stories: (1) Agent/LLM error spike, and (2) Temporal / infra outage, each with clear observability and runbooks."
  - "Auth is lightweight but real (API keys or bearer tokens), sufficient to tell a production-readiness story without heavy identity plumbing."
  - "Rate limiting is coarse-grained (per-API instance or simple token bucket) and configured via env vars, not a full multi-tenant quota system."
  - "Demo defaults stay easy to run locally; hardened settings are a documented toggle, not the default for every developer run."

must_haves:
  truths:
    - "There is a minimal but real authentication story for both API and Agent (e.g., API key or bearer token) that can be turned on/off via config and is visible in traces/logs."
    - "POST /chat has rate limiting and/or simple request budgeting, with clear 429/limit-hit behavior and metrics/logs showing when limits are reached."
    - "An incident where Agent/LLM errors spike is easy to reproduce and explain using traces, logs, and metrics, plus a short runbook."
    - "An incident where Temporal (or its DB) is unavailable is easy to reproduce and explain (API behavior, worker/Temporal errors, traces/logs/health), plus a short runbook."
    - "Docker-compose (or equivalent) clearly distinguishes demo-friendly defaults from more realistic hardened settings, including credentials and critical env vars."
    - "A productionization checklist doc exists that a senior engineer could reasonably use as a starting point to harden this system." 
  artifacts:
    - path: api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt
      provides: "Auth, rate limiting, and error-handling behavior for POST /chat, including metrics and logs for 4xx/5xx and limit hits."
    - path: api/src/main/kotlin/dev/otel/demo/api/routes/HealthRoutes.kt
      provides: "Health/readiness endpoints that reflect Temporal/Agent dependencies in a production-like way."
    - path: worker/src/main/kotlin/dev/otel/demo/worker/Main.kt
      provides: "Worker lifecycle wiring for graceful shutdown and dependency configuration."
    - path: worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt
      provides: "Clear error signaling for Agent/LLM failures and Temporal outage cases, with appropriate span status and logs."
    - path: agent/agent/main.py
      provides: "Agent auth enforcement, structured error responses, and logging for LLM/infra failures."
    - path: docker-compose.yml
      provides: "Demo-friendly defaults for local runs, with clear knobs for hardened modes."
    - path: docker-compose.override.yml
      provides: "Example hardened overrides (credentials, flags, resource limits) used in production-ish scenarios."
    - path: docs/INCIDENTS.md
      provides: "Two incident walkthroughs: Agent/LLM error spike and Temporal outage, each with steps, signals to check, and likely mitigations."
    - path: docs/PRODUCTION_CHECKLIST.md
      provides: "Concise productionization checklist covering auth, secrets, TLS, scaling, resilience, and observability."
    - path: CONFIG.md
      provides: "Documented env vars for auth, rate limiting, hardened infra, and incident toggles (where applicable)."
---

<objective>
Phase 5 adds a focused production-hardening layer on top of the existing demo, centered on two scriptable incident stories: (1) an Agent/LLM error spike, and (2) a Temporal/infra outage. It delivers basic but real auth and rate limiting, clearer dependency behavior and lifecycle handling, docker-compose defaults that distinguish "demo" vs "more realistic" modes, and documentation (runbooks + checklist) that lets you walk a candidate through "how would you productionize this?" without turning the repo into a full-blown platform.
</objective>

<execution_context>
Execute tasks in order 1–5. Keep the scope at "demo plus production-ish story": prioritize clarity and teaching value over completeness. Reuse existing observability work from Phases 2–4 (TraceQL walkthrough, broken observability flag, structured logs) and build on it for the two incident narratives. Auth/rate limiting should be minimal but non-trivial; infra changes should remain easy to run locally.
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 5 section)
@docs/TRACE_WALKTHROUGH.md
@docs/BROKEN_OBSERVABILITY.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@integration/README.md
@CONFIG.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce basic auth for API and Agent</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt, api/src/main/kotlin/dev/otel/demo/api/routes/HealthRoutes.kt, agent/agent/main.py, CONFIG.md, docs/INCIDENTS.md, docs/PRODUCTION_CHECKLIST.md</files>
  <action>
- **Auth mechanism:** Implement a lightweight auth model appropriate for a demo: e.g. shared **API key or bearer token** configured via env vars (`API_AUTH_TOKEN`, `AGENT_AUTH_TOKEN`). For Phase 5, prefer a single static token per service rather than per-user accounts.
- **API enforcement:** In `ChatRoutes.kt` (and any other write endpoints), read the incoming `Authorization` header (or `X-API-Key`) and enforce the configured token when `API_AUTH_ENABLED=true`. On failure, return 401/403 with a simple JSON body; set span status to error and log the auth failure (no secrets).
- **Agent enforcement:** In `agent.main`'s `/invoke` and `/health` (if desired), similarly enforce `AGENT_AUTH_ENABLED` + `AGENT_AUTH_TOKEN` on inbound requests from the worker or other clients. The worker config should be updated later (Task 3) to send the correct header.
- **Config and docs:** Add auth-related env vars to `CONFIG.md` under API and Agent sections (enabled flags + token names). In `docs/PRODUCTION_CHECKLIST.md`, include a short "Auth" section with the chosen model and TODOs for a real deployment (e.g. "replace static tokens with proper identity provider"). In `docs/INCIDENTS.md`, reference auth briefly where relevant (e.g. 401 storms as a possible variant of an incident).
  </action>
  <verify>Run API and Agent locally with `*_AUTH_ENABLED=false` and confirm behavior is unchanged for existing flows. Then set `*_AUTH_ENABLED=true` and provide the correct token from curl (and from the worker once wired) to confirm 2xx. Finally, send a request without/with wrong token and confirm 401/403, span status error, and a clear structured log line showing an auth failure.</verify>
  <done>API and Agent can be run with auth disabled (default) or enabled via env vars; when enabled, unauthenticated requests fail with clear 401/403 responses and corresponding traces/logs.</done>
</task>

<task type="auto">
  <name>Task 2: Add rate limiting and simple budgeting to POST /chat</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt, api/src/test/kotlin/dev/otel/demo/api/routes/ChatRoutesTest.kt, CONFIG.md, docs/INCIDENTS.md, docs/PRODUCTION_CHECKLIST.md</files>
  <action>
- **Rate limiting strategy:** Implement an in-process, coarse-grained limiter in the API for `POST /chat`, e.g. a simple token bucket or fixed window using an in-memory counter per-process. Support configuration via env vars such as `CHAT_RATE_LIMIT_PER_MINUTE` (e.g. default 60) and `CHAT_RATE_LIMIT_ENABLED`.
- **Behavior on limit hit:** When the limit is exceeded, return HTTP 429 with a short JSON error body. Record a span event (e.g. `rate.limit.hit`) and increment a counter metric if Phase 2 metrics are in place. Avoid complex, multi-tenant semantics; one global limiter per API instance is enough.
- **Tests:** Extend `ChatRoutesTest.kt` to cover the rate limit behavior: configure a low limit for the test (e.g. 1 per minute), send two quick requests, assert that the second returns 429 and the body indicates a limit hit.
- **Docs:** Update `CONFIG.md` with the rate limit env vars, and add a short entry to `docs/INCIDENTS.md` describing how 429 storms might look in logs/metrics (even if not the main incident story). In `docs/PRODUCTION_CHECKLIST.md`, add "Rate limiting / quotas" as an item, referencing these knobs as the starting point.
  </action>
  <verify>With rate limiting disabled, POST /chat behaves as today. With a low `CHAT_RATE_LIMIT_PER_MINUTE`, send rapid requests and confirm 429 behavior and (if metrics/logs are wired) see limit hits reflected. All API tests, including new ones, pass.</verify>
  <done>POST /chat is protected by a simple, configurable rate limiter that returns 429 on abuse, and the behavior is covered by tests and documented knobs.</done>
</task>

<task type="auto">
  <name>Task 3: Harden infra defaults and clarify demo vs "more realistic" modes</name>
  <files>docker-compose.yml, docker-compose.override.yml, integration/README.md, CONFIG.md, docs/PRODUCTION_CHECKLIST.md</files>
  <action>
- **Compose defaults:** Review `docker-compose.yml` and update any obviously insecure defaults (e.g. use non-default passwords, avoid `latest` tags where practical). Keep the file runnable out-of-the-box, but make security-relevant values configurable via env vars (documented in `CONFIG.md`).
- **Demo vs hardened:** Introduce a `docker-compose.override.yml` (or a commented section) that demonstrates a more production-like configuration: e.g. different credentials, explicit resource limits, maybe an environment flag that enables auth/rate limiting by default. The goal is to show "here is how you'd start moving from demo to real" without breaking the simple `docker compose up` story.
- **Docs:** In `integration/README.md`, add a short section that explains the difference between the base compose file and the override (or hardened settings), including when to use each. In `docs/PRODUCTION_CHECKLIST.md`, include a "Docker/infra defaults" section that calls out steps like "separate dev/prod configs, rotate creds, configure TLS, restrict exposed ports".
  </action>
  <verify>Run `docker compose up` as before and confirm the demo still starts with reasonable defaults. Optionally run `docker compose -f docker-compose.yml -f docker-compose.override.yml up` (or the documented variant) and confirm the hardened settings apply without breaking the demo.</verify>
  <done>Compose files clearly express both demo-friendly defaults and a path toward more realistic hardened settings, and the difference is documented.</done>
</task>

<task type="auto">
  <name>Task 4: Incident 1 — Agent/LLM error spike story</name>
  <files>worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt, worker/src/test/, agent/agent/main.py, agent/tests/, docs/TRACE_WALKTHROUGH.md, docs/INCIDENTS.md</files>
  <action>
- **Controlled failure mode:** Introduce a simple, config-driven way to force Agent/LLM failures for demo purposes (e.g. `AGENT_FORCE_ERROR_RATE` or a boolean `AGENT_FAIL_ALL_REQUESTS`). When enabled, the agent returns 5xx or raises an exception for `/invoke`. Keep this flag off by default.
- **Worker and API behavior:** Ensure `RunAgentActivity` and the API surface these failures clearly: spans should have error status with meaningful attributes (e.g. `error.type=agent_failure`), logs should indicate that the agent/LLM failed, and any retries (if configured) are visible in traces.
- **Tests:** Add unit tests for the agent and worker that simulate the forced-error config and assert: (a) proper status codes / exceptions, (b) spans/logs record errors as expected. No external LLM required.
- **Doc the incident:** In `docs/INCIDENTS.md`, add an "Incident 1: Agent/LLM error spike" section that describes: (1) how to turn on the failure flag, (2) what to look for in Grafana Tempo (error spans on agent/worker), (3) what logs/metrics show, and (4) what mitigations you might discuss in an interview (roll back flag, reduce load, add fallbacks, etc.). Reference `TRACE_WALKTHROUGH.md` where appropriate.
  </action>
  <verify>Enable the agent failure flag in a local run, send several POST /chat requests, and confirm a clear spike in errors in logs/traces (and metrics if available). Disable the flag and confirm behavior returns to normal. All new tests pass.</verify>
  <done>There is a repeatable Agent/LLM error-spike scenario with clear traces/logs and a documented incident walkthrough that you can use in interviews.</done>
</task>

<task type="auto">
  <name>Task 5: Incident 2 — Temporal / infra outage story + productionization checklist</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt, worker/src/main/kotlin/dev/otel/demo/worker/Main.kt, worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt, worker/src/test/, docs/INCIDENTS.md, docs/PRODUCTION_CHECKLIST.md</files>
  <action>
- **Outage behavior:** Define and implement how the system behaves when Temporal (or its DB) is unavailable. For example: when the API cannot start a workflow, it should return a clear 5xx with a message indicating a backend dependency problem; worker logs/spans should show connection failures to Temporal. Ensure this behavior is visible in traces (error spans), logs, and possibly health/readiness endpoints.
- **Health/readiness:** Optionally enhance health endpoints to reflect Temporal availability (e.g. a `/health/ready` that depends on Temporal), while keeping `/health` simple for local dev. This lets you tell a "readiness vs liveness" story in production contexts.
- **Doc the incident:** In `docs/INCIDENTS.md`, add "Incident 2: Temporal/infra outage" describing: (1) how to reproduce (e.g. stop Temporal container, then send /chat), (2) what breaks from the user's POV, (3) what traces/logs show, and (4) which mitigations you might consider (backoff, feature flag to bypass workflow, maintenance mode, etc.).
- **Productionization checklist:** Create `docs/PRODUCTION_CHECKLIST.md` (if not already) or extend it to include sections for: Auth, Rate limiting, Temporal & DB, Infra & Compose, Observability, and Operations (runbooks, on-call). Each item should be a short bullet list of concrete steps, using this demo as the baseline and noting where "real" systems would go further.
  </action>
  <verify>Bring down Temporal (or make it unreachable), send POST /chat, and confirm that the failure mode matches the documented story (status code, message, traces/logs). Restart Temporal and confirm recovery. Review `docs/PRODUCTION_CHECKLIST.md` and `docs/INCIDENTS.md` for coherence and usefulness.</verify>
  <done>There is a clear Temporal/infra outage incident story with a repeatable reproduction, observable signals, and a productionization checklist that ties Phase 5 together.</done>
</task>

</tasks>

<verification>
- Task 1: API and Agent can run with or without simple auth; when enabled, unauthorized traffic yields 401/403 with clear traces/logs.
- Task 2: POST /chat is rate-limited by configurable env vars, with tested 429 behavior and documented knobs.
- Task 3: Compose files and docs distinguish demo vs more realistic hardened modes without breaking local dev.
- Task 4: Agent/LLM error-spike incident is reproducible and well-documented with traces/logs/metrics.
- Task 5: Temporal/infra outage incident is reproducible and well-documented; PRODUCTION_CHECKLIST gives a credible starting point for hardening.
</verification>

<success_criteria>
- Phase 5 goal achieved: the repo now supports a credible "how would you productionize this?" conversation via two concrete incident stories, basic auth and rate limiting, hardened-but-runnable infra defaults, and a concise productionization checklist.
- An executor can implement Phase 5 using this plan without ambiguity and with a clear focus on teaching value rather than exhaustive hardening.
</success_criteria>

<output>
After completion, create .planning/phases/05-production/05-01-SUMMARY.md summarizing what was implemented and any deviations.
</output>
