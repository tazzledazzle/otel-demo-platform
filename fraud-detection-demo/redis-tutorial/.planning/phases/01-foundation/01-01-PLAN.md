---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - 01_foundation.py
  - README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Learner can run one command and see the example execute"
    - "Script connects to Redis (ping) and shows connection success or clear error"
    - "Script performs set/get and learner sees the read value in output"
    - "Script demonstrates one extra operation (TTL via setex/ttl) and output shows it"
    - "Prerequisites and connection (URL/env) are documented in README"
  artifacts:
    - path: requirements.txt
      provides: "redis client dependency"
      contains: "redis"
    - path: 01_foundation.py
      provides: "Single runnable example script"
      min_lines: 25
    - path: README.md
      provides: "Prerequisites and run instructions"
      contains: "Redis"
  key_links:
    - from: 01_foundation.py
      to: "redis.from_url"
      via: "REDIS_URL env with decode_responses=True"
      pattern: "from_url.*decode_responses"
    - from: 01_foundation.py
      to: "set/get and setex/ttl"
      via: "r.set, r.get, r.setex, r.ttl and print"
      pattern: "r\\.(set|get|setex|ttl)"
---

<objective>
Deliver one runnable Redis example: connect via URL/env, set/get, and TTL (setex/ttl), so the learner runs a single script and sees read/write and expiration in action.

Purpose: Fulfill Phase 1 goal — engineer-oriented, code-first foundation with clear connection setup.
Output: requirements.txt, 01_foundation.py, README.md.
</objective>

<execution_context>
@/Users/terenceschumacher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies and runnable script</name>
  <files>requirements.txt, 01_foundation.py</files>
  <action>
Create requirements.txt with redis>=5.0,<8 (per RESEARCH standard stack).

Create 01_foundation.py at repo root (single script per CONTEXT and RESEARCH). Implementation:
- Import os and redis.
- Build client with redis.from_url(os.getenv('REDIS_URL', 'redis://localhost:6379'), decode_responses=True). Do not hand-roll URL parsing; use from_url only.
- Wrap connection check in try/except: call r.ping(). On connection failure, print a clear message (e.g. "Could not connect to Redis. Is Redis running? Try: docker run -p 6379:6379 redis:latest") and exit non-zero; on success continue.
- Perform and print: r.set('greeting', 'Hello, Redis!'); print(r.get('greeting')).
- Add optional structure: TTL. Use r.setex('temp_key', 60, 'expires in 60 seconds'); print(r.ttl('temp_key')). Do not add hash or list in this task; TTL only.
- Use decode_responses=True on from_url (required for learner-facing output; RESEARCH pitfall).
- No custom ConnectionPool, no pub/sub, no production patterns (deferred).
  </action>
  <verify>From repo root: pip install -r requirements.txt then python 01_foundation.py (with Redis listening on localhost:6379, or REDIS_URL set). Exit code 0; stdout contains the greeting value and ttl value (e.g. 60).</verify>
  <done>requirements.txt exists with redis pin; 01_foundation.py runs, connects via REDIS_URL/default URL, prints set/get result and TTL; connection errors produce clear message and non-zero exit.</done>
</task>

<task type="auto">
  <name>Task 2: README prerequisites and run instructions</name>
  <files>README.md</files>
  <action>
Create README.md at repo root. Include:
- Short project description (tutorial for Redis; Phase 1 = one runnable example).
- Prerequisites: Redis running (e.g. "Start Redis: docker run -p 6379:6379 redis:latest" or local install); Python 3.9+; pip install -r requirements.txt.
- Connection: optional REDIS_URL env var for cloud or non-default URL; default redis://localhost:6379.
- How to run: python 01_foundation.py.
- No clustering, pub/sub, or production setup (out of scope for Phase 1).
  </action>
  <verify>README.md exists; contains the words Redis, prerequisites, 01_foundation.py (or "run the script"); reader can follow steps to run the example.</verify>
  <done>README documents Redis and Python prerequisites, REDIS_URL/default URL, and run command for 01_foundation.py.</done>
</task>

</tasks>

<verification>
- requirements.txt present; pip install -r requirements.txt succeeds.
- With Redis available: python 01_foundation.py exits 0; output includes the get value and ttl (e.g. 60).
- Without Redis: script exits non-zero and prints a clear connection-failure message.
- README lists prerequisites and run instructions.
</verification>

<success_criteria>
One runnable example: learner runs python 01_foundation.py and sees connect, set/get, and TTL output; prerequisites and connection (URL/env) documented in README.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-foundation-01-SUMMARY.md
</output>
