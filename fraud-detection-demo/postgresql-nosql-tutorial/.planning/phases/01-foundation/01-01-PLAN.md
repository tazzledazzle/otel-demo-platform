---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - postgres_example.py
autonomous: true
must_haves:
  truths:
    - "Learner can run the Postgres example and see a result (e.g. timestamp or query output)"
    - "Connection string comes from environment (DATABASE_URL), not hardcoded"
  artifacts:
    - path: requirements.txt
      provides: "psycopg and pymongo deps for both examples"
      contains: "psycopg[binary]"
    - path: .env.example
      provides: "Documented env var placeholders"
      contains: "DATABASE_URL"
    - path: postgres_example.py
      provides: "Runnable PostgreSQL connect + query example"
      min_lines: 15
  key_links:
    - from: postgres_example.py
      to: env
      via: "os.environ.get(\"DATABASE_URL\", ...)"
      pattern: "os\\.environ\\.get.*DATABASE_URL"
    - from: postgres_example.py
      to: psycopg
      via: "with psycopg.connect(...) as conn"
      pattern: "psycopg\\.connect"
---

<objective>
Deliver project dependency setup and one runnable PostgreSQL example: connect via env, run at least one query, print result. No hybrid or NoSQL in this plan.
Purpose: Satisfy Phase 1 "one runnable example for PostgreSQL" with clear setup and env-based config.
Output: requirements.txt, .env.example, postgres_example.py.
</objective>

<execution_context>
@/Users/terenceschumacher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project setup — requirements and env example</name>
  <files>postgresql-nosql-tutorial/requirements.txt</files>
  <action>
Create requirements.txt in the tutorial root (postgresql-nosql-tutorial/) with:
- psycopg[binary] (so learners need no system libpq; use psycopg 3, not psycopg2)
- pymongo

Create .env.example in the tutorial root with placeholder lines (comments OK):
- DATABASE_URL=postgresql://localhost/postgres
- MONGODB_URI=mongodb://localhost:27017

Use exact package names from RESEARCH; no version pins unless required for compatibility.
  </action>
  <verify>From tutorial root: cat requirements.txt shows psycopg[binary] and pymongo; cat .env.example shows DATABASE_URL and MONGODB_URI.</verify>
  <done>requirements.txt and .env.example exist; pip install -r requirements.txt runs without error from repo root or tutorial dir.</done>
</task>

<task type="auto">
  <name>Task 2: PostgreSQL runnable example</name>
  <files>postgresql-nosql-tutorial/postgres_example.py</files>
  <action>
Create postgres_example.py in the tutorial root that:
- Imports os and psycopg (package name psycopg, not psycopg3).
- Reads DSN from os.environ.get("DATABASE_URL", "postgresql://localhost/postgres").
- Uses "with psycopg.connect(dsn) as conn:" (context manager; in psycopg3 this closes the connection on exit—correct for single script).
- Executes at least one query: e.g. conn.execute("SELECT now()").fetchone() or SELECT 1; print the result.
- No hardcoded connection strings in source. No async. Use parameterized queries for any user input (e.g. %s with tuple) per RESEARCH anti-patterns.
  </action>
  <verify>From tutorial root, with Postgres running and DATABASE_URL set (or default): python postgres_example.py — exits 0 and prints a result (e.g. timestamp or 1).</verify>
  <done>Script runs and prints query output; connection uses env; context manager used for connect.</done>
</task>

</tasks>

<verification>
- requirements.txt contains psycopg[binary] and pymongo; pip install -r postgresql-nosql-tutorial/requirements.txt succeeds.
- postgres_example.py runs and prints one query result when Postgres is available and DATABASE_URL is valid (or default).
</verification>

<success_criteria>
- One runnable PostgreSQL example; connection from env; learner can run and see result.
- Setup artifacts (requirements.txt, .env.example) in place for Phase 1.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-foundation-01-SUMMARY.md
</output>
