---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mongodb_example.py
  - README.md
autonomous: true
user_setup:
  - service: postgresql
    why: "PostgreSQL example requires a running Postgres instance"
    env_vars:
      - name: DATABASE_URL
        source: "Default postgresql://localhost/postgres or set in .env"
    note: "Document in README: local install, Docker, or cloud"
  - service: mongodb
    why: "MongoDB example requires a running MongoDB instance"
    env_vars:
      - name: MONGODB_URI
        source: "Default mongodb://localhost:27017 or MongoDB Atlas connection string"
    note: "Document in README: local, Docker, or Atlas free tier"
must_haves:
  truths:
    - "Learner can run the MongoDB example and see a result (e.g. inserted then found document)"
    - "Prerequisites and run steps for both Postgres and NoSQL examples are documented"
    - "Connection string for MongoDB comes from environment (MONGODB_URI)"
  artifacts:
    - path: mongodb_example.py
      provides: "Runnable MongoDB connect + insert + find example"
      min_lines: 20
    - path: README.md
      provides: "Prerequisites, env vars, and run steps for both examples"
      contains: "DATABASE_URL"
  key_links:
    - from: mongodb_example.py
      to: env
      via: "os.environ.get(\"MONGODB_URI\", ...)"
      pattern: "MONGODB_URI"
    - from: README.md
      to: postgres_example.py
      via: "Run steps"
      pattern: "postgres_example"
    - from: README.md
      to: mongodb_example.py
      via: "Run steps"
      pattern: "mongodb_example"
---

<objective>
Deliver one runnable MongoDB example and README: connect via env, insert_one + find_one, print result; document prerequisites and run steps for both Postgres and MongoDB examples.
Purpose: Satisfy Phase 1 "one runnable example for one NoSQL store" and "prerequisites and run steps documented."
Output: mongodb_example.py, README.md.
</objective>

<execution_context>
@/Users/terenceschumacher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MongoDB runnable example</name>
  <files>postgresql-nosql-tutorial/mongodb_example.py</files>
  <action>
Create mongodb_example.py in the tutorial root that:
- Uses PyMongo (from pymongo import MongoClient). Sync client only; no async.
- Reads URI from os.environ.get("MONGODB_URI", "mongodb://localhost:27017").
- Connects with MongoClient(uri), gets a database (e.g. "tutorial") and collection (e.g. "demo").
- Performs insert_one (e.g. {"name": "example", "value": 1}) then find_one with same criteria; prints the document.
- Calls client.close() when done (or use context manager if preferred). No hardcoded URIs in source.
  </action>
  <verify>From tutorial root, with MongoDB running and MONGODB_URI set (or default): python mongodb_example.py — exits 0 and prints the found document (e.g. name and value).</verify>
  <done>Script runs and prints document from find_one; connection uses env; insert_one and find_one used.</done>
</task>

<task type="auto">
  <name>Task 2: README — prerequisites and run steps</name>
  <files>postgresql-nosql-tutorial/README.md</files>
  <action>
Create or replace README.md in the tutorial root with:
- Brief project description (PostgreSQL + NoSQL tutorial; Phase 1 = two separate runnable examples).
- Prerequisites: PostgreSQL running (local install, Docker, or cloud); MongoDB running (local, Docker, or MongoDB Atlas). Optionally link to Atlas or give one Docker run command per service.
- Environment variables: DATABASE_URL (default postgresql://localhost/postgres), MONGODB_URI (default mongodb://localhost:27017). Mention .env.example.
- Run steps: pip install -r requirements.txt; set env vars if needed; python postgres_example.py; python mongodb_example.py. Keep engineer-oriented and code-first.
  </action>
  <verify>README.md exists and contains the words DATABASE_URL, MONGODB_URI, postgres_example, mongodb_example, and prerequisites or run steps.</verify>
  <done>README documents prerequisites, env vars, and run steps for both examples.</done>
</task>

</tasks>

<verification>
- mongodb_example.py runs and prints a document when MongoDB is available and MONGODB_URI is valid (or default).
- README lists prerequisites (Postgres + MongoDB), env vars, and commands to run both scripts.
</verification>

<success_criteria>
- One runnable MongoDB example; connection from env; learner can run and see result.
- README enables a new learner to install deps, set env, and run both examples.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-foundation-02-SUMMARY.md
</output>
