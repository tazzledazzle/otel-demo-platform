---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - settings.gradle.kts
  - gradle/wrapper/gradle-wrapper.properties
  - gradle/wrapper/gradle-wrapper.jar
  - gradlew
  - gradlew.bat
  - src/main/java/com/example/demo/DemoApplication.java
  - src/main/java/com/example/demo/HealthController.java
autonomous: true

must_haves:
  truths:
    - "Learner can build the project with one command (./gradlew build)"
    - "Learner can run the service with one command (./gradlew bootRun)"
    - "Service exposes GET /health"
    - "Requesting GET /health returns a simple response (e.g. OK)"
    - "Project uses a named root package (com.example.demo), not default package"
  artifacts:
    - path: build.gradle.kts
      provides: "Gradle build with Spring Boot 3.x and spring-boot-starter-web"
      contains: "spring-boot-starter-web"
    - path: settings.gradle.kts
      provides: "Project name and root project config"
    - path: src/main/java/com/example/demo/DemoApplication.java
      provides: "Main class with @SpringBootApplication"
      contains: "@SpringBootApplication"
    - path: src/main/java/com/example/demo/HealthController.java
      provides: "GET /health endpoint"
      contains: "@GetMapping(\"/health\")"
  key_links:
    - from: "src/main/java/com/example/demo/HealthController.java"
      to: "DemoApplication component scan"
      via: "same package or subpackage of main class"
      pattern: "package com\\.example\\.demo"
    - from: "build.gradle.kts"
      to: "Spring Boot runtime"
      via: "org.springframework.boot plugin and bootRun task"
      pattern: "bootRun|spring-boot"
---

<objective>
Deliver one runnable JVM service (Java) with a single HTTP endpoint (GET /health). Use Spring Boot 3.x with Gradle per RESEARCH recommendation. Single-command run: ./gradlew bootRun; success = curl http://localhost:8080/health returns a simple response (e.g. OK).

Purpose: Foundation for the tutorial so learners can run a minimal service before adding persistence or resilience.
Output: Gradle project with wrapper, Spring Boot app in package com.example.demo, one REST controller for /health.
</objective>

<execution_context>
@/Users/terenceschumacher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Spring Boot project with Gradle</name>
  <files>build.gradle.kts, settings.gradle.kts, gradle/wrapper/gradle-wrapper.properties, gradle/wrapper/gradle-wrapper.jar, gradlew, gradlew.bat, src/main/java/com/example/demo/DemoApplication.java</files>
  <action>
Create a minimal Spring Boot 3.x (Java) project with Gradle.

- **Build:** Use Kotlin DSL. Add plugins: `org.gradle.java`, `org.springframework.boot` (version 3.2.x or 3.3.x from current Spring docs), `io.spring.dependency-management`. Set group `com.example`, version `0.0.1-SNAPSHOT`, Java 17 (sourceCompatibility). Single dependency: `org.springframework.boot:spring-boot-starter-web`. Repositories: mavenCentral().
- **Wrapper:** Include Gradle wrapper (gradlew, gradlew.bat, gradle/wrapper/*) so learners run without installing Gradle. Use Gradle 8.x. Ensure gradlew is executable (chmod +x gradlew).
- **Package:** Use named root package `com.example.demo`. Do NOT use the default package (RESEARCH: component scan pitfalls).
- **Main class:** Create `src/main/java/com/example/demo/DemoApplication.java` with `@SpringBootApplication` and `public static void main(String[] args)` calling `SpringApplication.run(DemoApplication.class, args)`.

Do not add persistence, actuator, or extra dependencies. Optional: `src/main/resources/application.properties` with `server.port=8080` only if you want to make port explicit.
  </action>
  <verify>Run `./gradlew build` from project root; build succeeds. Run `./gradlew bootRun`; application starts without error (then stop it).</verify>
  <done>Project builds and Spring Boot application starts; no HTTP endpoint yet.</done>
</task>

<task type="auto">
  <name>Task 2: Add GET /health endpoint</name>
  <files>src/main/java/com/example/demo/HealthController.java</files>
  <action>
Add a single HTTP endpoint: GET /health returning a plain text response.

- Create `src/main/java/com/example/demo/HealthController.java` in package `com.example.demo` (so component scan finds it).
- Annotate class with `@RestController`.
- Add one method: `@GetMapping("/health")` returning `String` (e.g. `"OK"`). Use `org.springframework.web.bind.annotation.GetMapping` and `@RestController`.

Do not add JSON, database, or other endpoints. Per CONTEXT: one endpoint, simple response.
  </action>
  <verify>Run `./gradlew bootRun`, then in another terminal run `curl -s http://localhost:8080/health`. Response body must be `OK` (or the chosen string). Stop bootRun after verification.</verify>
  <done>GET http://localhost:8080/health returns 200 with body "OK".</done>
</task>

</tasks>

<verification>
- From project root: `./gradlew build` exits 0.
- From project root: `./gradlew bootRun` starts the app; `curl -s http://localhost:8080/health` returns `OK`.
- No classes in the default package; main and controller are under `com.example.demo`.
</verification>

<success_criteria>
- One runnable JVM service; single-command run (./gradlew bootRun).
- One HTTP endpoint: GET /health returning a simple response (e.g. OK).
- Clear project layout with named root package; engineer-oriented, minimal boilerplate.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
