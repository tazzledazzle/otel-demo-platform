# Technology Stack

**Analysis Date:** 2025-02-25

## Languages

**Primary:** Not chosen. PROJECT.md specifies "GraphQL (e.g. Apollo, GraphQL-Java) and/or REST; any language." Stack will be defined when roadmap phases are implemented.

**Secondary:** Not applicable.

## Runtime

**Environment:** None. No runtime until a phase introduces a server or client runtime (Node, JVM, Python, etc.).

**Package Manager:** None. No lockfile or manifest at project root. When code is added, use the appropriate manager (npm/yarn/pnpm, pip/poetry, Gradle/Maven) and document here.

## Frameworks

**Core:** To be defined per phase (e.g. Apollo Server, Express, FastAPI, Spring, GraphQL-Java).

**Testing:** Not applicable. Document in TESTING.md when a test framework is added.

**Build/Dev:** Not applicable.

## Key Dependencies

**Critical:** None. Future tutorial dependencies (GraphQL runtime, HTTP server, client libraries) to be listed here when introduced.

**Infrastructure:** None.

## Configuration

**Environment:** Only `graphql-rest-tutorial/config.json` exists (GSD workflow). No .env or app config. When APIs are added, document required env vars and config files here.

**Build:** No build config. Add when the stack is chosen (e.g. tsconfig.json, package.json scripts, pyproject.toml).

## Platform Requirements

**Development:** None until stack is chosen.

**Production:** Tutorial-focused; production deployment is out of scope unless a phase specifies it.

---

*Stack analysis: 2025-02-25*
