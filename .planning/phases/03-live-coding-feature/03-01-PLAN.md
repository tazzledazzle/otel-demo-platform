---
phase: 03-live-coding-feature
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - otel-demo-platform/agent/agent/chain.py
  - otel-demo-platform/agent/tests/test_chain.py
autonomous: true

must_haves:
  truths:
    - "The chosen pipeline step is clearly identified and scoped for in-session implementation"
    - "A candidate can implement it following existing patterns (new chain step in chain.py)"
    - "The change is demonstrable (test passes; new step observable in chain output or trace)"
  artifacts:
    - path: otel-demo-platform/agent/agent/chain.py
      provides: "Pipeline with new runnable step (prompt | llm | step)"
      contains: "chain = prompt | llm"
    - path: otel-demo-platform/agent/tests/test_chain.py
      provides: "Test that new step is exercised and output is present"
      min_lines: 5
  key_links:
    - from: otel-demo-platform/agent/agent/chain.py
      to: "get_agent_executor"
      via: "chain passed to _Executor"
      pattern: "prompt \\| llm \\| .*"
    - from: otel-demo-platform/agent/tests/test_chain.py
      to: "get_agent_executor"
      via: "invoke and assert output"
      pattern: "invoke.*output"
---

<objective>
Implement one new pipeline step in the Python agent (Option B from LIVE_FEATURE.md): a runnable step after the LLM (e.g. summarize) so the chain becomes `prompt | llm | new_step`. Add a test that exercises the chain and asserts observable behavior. No new Temporal activity; change is demonstrable via test and (when run with stack) via trace in Grafana.
</objective>

<execution_context>
@/Users/terenceschumacher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@otel-demo-platform/docs/LIVE_FEATURE.md
@otel-demo-platform/agent/agent/chain.py
@otel-demo-platform/agent/tests/test_chain.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new chain step in chain.py</name>
  <files>otel-demo-platform/agent/agent/chain.py</files>
  <action>
    Add one new runnable step after the LLM in the agent pipeline (Option B per LIVE_FEATURE.md).

    1. Define a runnable that accepts the LLM output (AIMessage) and returns a value that has .content (e.g. AIMessage or an object with content), so _Executor.invoke's existing logic (result.content) still works. Examples:
       - A short "summarize" step: e.g. pass-through, or truncate to first N characters and return as AIMessage(content=...).
       - Use langchain_core.runnables.RunnableLambda or a small callable; ensure the chain passes the message through so the executor receives something with .content.

    2. Compose the chain as: chain = prompt | llm | &lt;new_step&gt; (replace the current "chain = prompt | llm").

    3. Do not add a new Temporal workflow or activity. Do not change _Executor or the tools list unless needed for the new step.

    Existing pattern: prompt and llm are already composed; add one more pipe segment. Keep the same get_agent_executor() API.
  </action>
  <verify>Run: cd otel-demo-platform/agent && python -c "from agent.chain import get_agent_executor; e = get_agent_executor(); print(e.invoke({'input': 'hi'}))" — must print a dict with key "output".</verify>
  <done>chain.py contains prompt | llm | new_step and get_agent_executor() returns an executor whose invoke() returns {"output": ...}.</done>
</task>

<task type="auto">
  <name>Task 2: Add test for new step / chain output</name>
  <files>otel-demo-platform/agent/tests/test_chain.py</files>
  <action>
    Add a test that exercises the chain including the new step and asserts observable behavior.

    1. Follow existing patterns in test_chain.py: use unittest.mock.patch to mock get_llm (or ChatOllama) so tests do not require Ollama.

    2. Add one test that: invokes get_agent_executor().invoke({"input": "..."}) with a mock LLM returning a known message, and asserts that the returned dict contains "output" and that the output is non-empty (or contains expected content if the new step transforms it in a testable way). This demonstrates the new step is in the pipeline and behavior is observable.

    3. Keep test_search_tool and test_get_agent_executor_returns_executor and test_executor_invoke; add the new test (e.g. test_executor_invoke_with_new_step or test_chain_includes_summarize_step) without removing existing tests.
  </action>
  <verify>Run: cd otel-demo-platform/agent && pytest tests/test_chain.py -v</verify>
  <done>pytest tests/test_chain.py passes; new test asserts chain output with new step.</done>
</task>

</tasks>

<verification>
- From repo root: cd otel-demo-platform/agent && pytest tests/test_chain.py -v
- Optional (demonstration): start stack + agent, send one POST /invoke, open Grafana and confirm trace; new step may appear as additional span if LangChain/OTel instrumentation is present.
</verification>

<success_criteria>
1. The chosen pipeline step is clearly identified and scoped (one new runnable in chain.py).
2. Implementation follows existing patterns (chain composition, executor unchanged; test mocks match test_chain.py).
3. The change is demonstrable: pytest passes and executor returns {"output": ...} when invoked.
</success_criteria>

<output>
After completion, create .planning/phases/03-live-coding-feature/03-01-SUMMARY.md
</output>
