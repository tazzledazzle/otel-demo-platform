---
phase: 03-temporal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/src/main/kotlin/dev/otel/demo/contracts/AgentWorkflow.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/AgentWorkflowImpl.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivityInterface.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/Main.kt
  - worker/src/test/kotlin/dev/otel/demo/worker/AgentWorkflowImplTest.kt
  - worker/src/test/kotlin/dev/otel/demo/worker/RunAgentActivityTest.kt
  - api/src/main/kotlin/dev/otel/demo/api/TemporalClientFactory.kt
  - api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt
  - api/src/main/kotlin/dev/otel/demo/api/models/Models.kt
  - docs/TEMPORAL_WALKTHROUGH.md
  - integration/README.md
autonomous: true

must_haves:
  truths:
    - "AgentWorkflow remains a simple request/response contract, with explicit workflow and activity timeouts and retry policies visible in code."
    - "At least one workflow run executes multiple distinct activities (pre-process, agent call, post-process) in sequence, visible as separate steps in Temporal history and in traces."
    - "A controlled failure path (triggered by a special message) demonstrates Temporal retries with multiple activity attempts visible in Temporal Web and Tempo."
    - "POST /chat exposes workflow metadata (workflowId and task queue) in the API response and/or structured logs so callers can correlate with Temporal Web and Grafana."
    - "Worker tests using Temporal's testing utilities validate a workflow end-to-end without calling real Temporal service or the Python agent."
    - "Temporal-centric docs explain how to inspect workflow histories, identify stuck or long-running workflows, and relate workflow state to traces."
  artifacts:
    - path: contracts/src/main/kotlin/dev/otel/demo/contracts/AgentWorkflow.kt
      provides: "Temporal workflow interface for agent chat with stable run(message: String): String contract"
    - path: worker/src/main/kotlin/dev/otel/demo/worker/AgentWorkflowImpl.kt
      provides: "Workflow implementation with explicit ActivityOptions, retry policy, and multi-step activity chain"
    - path: worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt
      provides: "Activity implementation that calls the agent and supports simulated failures for retry demos"
    - path: worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivityInterface.kt
      provides: "Activity interface used by workflow and tests"
    - path: worker/src/test/kotlin/dev/otel/demo/worker/AgentWorkflowImplTest.kt
      provides: "Temporal TestWorkflowEnvironment-based test for AgentWorkflow without real agent"
    - path: api/src/main/kotlin/dev/otel/demo/api/TemporalClientFactory.kt
      provides: "Workflow client with explicit workflow timeouts and access to workflow metadata"
    - path: api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt
      provides: "Chat route that starts workflows and surfaces workflow metadata to callers/logs"
    - path: api/src/main/kotlin/dev/otel/demo/api/models/Models.kt
      provides: "ChatResponse model including optional workflow metadata fields"
    - path: docs/TEMPORAL_WALKTHROUGH.md
      provides: "Temporal-focused walkthrough for inspecting workflows, retries, and long-running patterns"
    - path: integration/README.md
      provides: "Link from integration/e2e docs into Temporal walkthrough and retry demo"
  key_links:
    - from: api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt
      to: api/src/main/kotlin/dev/otel/demo/api/TemporalClientFactory.kt
      via: "AgentWorkflowClient that starts workflows with configured timeouts and returns metadata"
    - from: worker/src/main/kotlin/dev/otel/demo/worker/AgentWorkflowImpl.kt
      to: worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt
      via: "Activity stub with explicit ActivityOptions and retry policy"
    - from: worker/src/main/kotlin/dev/otel/demo/worker/AgentWorkflowImpl.kt
      to: "new pre/post-processing activities"
      via: "multi-step workflow chain demonstrating Temporal composition"
    - from: docs/TEMPORAL_WALKTHROUGH.md
      to: "Temporal Web UI and Grafana Tempo"
      via: "instructions for locating workflows, retries, and stuck executions"
---

<objective>
Phase 3 moves the demo from a single opaque workflow call to a set of clear Temporal patterns: explicit timeouts and retries, a multi-step workflow composed of several activities, a controlled failure/retry scenario, API-level workflow metadata exposure, Temporal testing for worker logic, and documentation focused on Temporal troubleshooting and exploration.
</objective>

<execution_context>
Execute tasks in order 1–6. Each task has explicit files, action, verify, and done criteria. Honor prior phase decisions: keep AgentWorkflow as the primary chat workflow contract; preserve existing observability behavior (TraceQL-first docs, OTEL_DISABLE_TRACING flag) while layering Temporal patterns on top.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@contracts/src/main/kotlin/dev/otel/demo/contracts/AgentWorkflow.kt
@worker/src/main/kotlin/dev/otel/demo/worker/AgentWorkflowImpl.kt
@worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt
@worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivityInterface.kt
@worker/src/main/kotlin/dev/otel/demo/worker/Main.kt
@api/src/main/kotlin/dev/otel/demo/api/TemporalClientFactory.kt
@api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt
@api/src/main/kotlin/dev/otel/demo/api/models/Models.kt
@docs/TRACE_WALKTHROUGH.md
@integration/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine AgentWorkflow contract, timeouts, and retry policies</name>
  <files>contracts/src/main/kotlin/dev/otel/demo/contracts/AgentWorkflow.kt, worker/src/main/kotlin/dev/otel/demo/worker/AgentWorkflowImpl.kt, api/src/main/kotlin/dev/otel/demo/api/TemporalClientFactory.kt</files>
  <action>
- **Keep the contract stable:** Confirm `AgentWorkflow` in `contracts/AgentWorkflow.kt` keeps the existing workflow interface and method signature: `fun run(message: String): String`. Do not add parameters or change the return type; this contract remains the simple request/response teaching example.
- **Add explicit activity timeouts and retry policy in the workflow implementation:** In `AgentWorkflowImpl.run`, replace the current plain `ActivityOptions` with a clearly configured options object:
  - Use `ActivityOptions.newBuilder()` to set a realistic-but-short demo timeout such as `setScheduleToCloseTimeout(Duration.ofSeconds(60))` (or keep `StartToClose` if preferred, but keep the timeout under ~2 minutes for demos).
  - Add a `RetryOptions` instance via `.setRetryOptions(...)` with fields like: initial interval ~2–5 seconds, backoff coefficient ~2.0, maximum attempts 3–5, and an overall `maximumInterval` under the activity timeout. This makes retries explicit and visible in Temporal history.
  - Continue creating the `RunAgentActivityInterface` stub via `Workflow.newActivityStub(RunAgentActivityInterface::class.java, options)` so existing behavior is preserved.
- **Add workflow-level timeouts at the client boundary:** In `TemporalClientFactory.AgentWorkflowClient.runAgentWorkflow`, when building `WorkflowOptions`, augment the builder with:
  - `setWorkflowRunTimeout(Duration.ofMinutes(3))` (or similar) to bound how long the overall workflow may run.
  - `setWorkflowTaskTimeout(Duration.ofSeconds(30))` to bound how long each workflow task can be in-flight.
  - Keep task queue and workflow ID behavior the same (task queue from env or default "agent-task-queue"; workflow ID pattern `agent-{timestamp}`).
- **Ensure configuration is discoverable:** Where appropriate, use small private constants in `AgentWorkflowImpl` and/or `TemporalClientFactory` to hold the timeout/retry values so the Temporal walkthrough doc can reference them by name (e.g. `AGENT_ACTIVITY_TIMEOUT`, `AGENT_ACTIVITY_RETRY_OPTIONS`).
  </action>
  <verify>Inspect AgentWorkflow.kt to confirm the interface is unchanged. In AgentWorkflowImpl, confirm ActivityOptions now include an explicit timeout and RetryOptions. In TemporalClientFactory, confirm WorkflowOptions include workflowRunTimeout and workflowTaskTimeout. Run ./gradlew :worker:test :api:test and confirm tests still pass.</verify>
  <done>AgentWorkflow remains a simple run(message: String): String contract; the workflow implementation and client clearly configure workflow and activity timeouts and retry policies in code.</done>
</task>

<task type="auto">
  <name>Task 2: Add a multi-step workflow scenario (pre-process → agent call → post-process)</name>
  <files>worker/src/main/kotlin/dev/otel/demo/worker/AgentWorkflowImpl.kt, worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivityInterface.kt, worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt, worker/src/main/kotlin/dev/otel/demo/worker/Main.kt</files>
  <action>
- **Introduce pre- and post-processing activities:** In the worker module, add two new activity interfaces and implementations that will be chained by `AgentWorkflowImpl`:
  - `PreprocessActivityInterface` with `@ActivityInterface` and a method like `@ActivityMethod fun preprocess(message: String): String`.
  - `PostprocessActivityInterface` with `@ActivityInterface` and a method like `@ActivityMethod fun postprocess(reply: String): String`.
  - Implement `PreprocessActivity` to perform simple, deterministic transformations that are easy to see in Temporal history and traces (e.g., trimming whitespace, tagging the message, or prefixing with `"preprocessed:"`).
  - Implement `PostprocessActivity` to wrap or annotate the agent reply (e.g., `"postprocessed:$reply"`). Keep these implementations side-effect-free so they are safe for retries.
- **Update AgentWorkflowImpl to chain multiple activities:** In `AgentWorkflowImpl.run`:
  - Reuse or extend the existing `ActivityOptions` (from Task 1) to create three stubs: one for `PreprocessActivityInterface`, one for `RunAgentActivityInterface`, and one for `PostprocessActivityInterface`.
  - Implement the workflow logic as a clear sequence:
    - Call `preprocessActivity.preprocess(message)` to obtain a cleaned/annotated input.
    - Call `runAgentActivity.run(preprocessedMessage)` to invoke the agent.
    - Call `postprocessActivity.postprocess(rawReply)` to obtain the final reply.
  - Return the post-processed reply from `run`.
- **Register new activities with the worker:** In `worker/Main.kt`, extend `worker.registerActivitiesImplementations` to include instances of `PreprocessActivity` and `PostprocessActivity` alongside `RunAgentActivity`, ensuring they share the same task queue and any necessary configuration.
- **Keep the external contract unchanged:** Ensure the workflow still exposes the same signature through `AgentWorkflow.run(message: String): String` and that API behavior (ChatResponse.reply) remains a plain string (albeit possibly wrapped/annotated by the post-processing step).
  </action>
  <verify>Run the stack (infra, agent, worker, API) and send POST /chat with a simple message. In Temporal Web UI, inspect the workflow history and confirm three activity executions (preprocess, run agent, postprocess) run in order. In Tempo, open the trace for that request and confirm worker spans/events make it clear that multiple activities ran for the workflow.</verify>
  <done>AgentWorkflowImpl now chains at least three activities (pre-process, agent call, post-process) in a single workflow run, and this composition is visible in Temporal history and traces without changing the public workflow contract.</done>
</task>

<task type="auto">
  <name>Task 3: Demonstrate workflow failure and retry behavior via a controlled failure mode</name>
  <files>worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt, worker/src/main/kotlin/dev/otel/demo/worker/AgentWorkflowImpl.kt, docs/TEMPORAL_WALKTHROUGH.md</files>
  <action>
- **Add a deterministic failure trigger in RunAgentActivity:** In `RunAgentActivity.run`, use Temporal's activity execution context to access attempt metadata:
  - Obtain `val ctx = Activity.getExecutionContext()` and `val attempt = ctx.info.attempt`.
  - Introduce a simple, opt-in failure trigger based on the message content, e.g. treat messages starting with a prefix like `"fail:"` as a retry demo.
  - Before calling `agentInvoke`, if `message.startsWith("fail:")` and `attempt` is below a chosen threshold (e.g. &lt; 3), add a span event like `"agent.simulated_failure"` and throw a `RuntimeException("Simulated agent failure for retry demo (attempt=$attempt)")` instead of calling the real agent.
  - For attempts at or above the threshold, call the real agent and complete successfully so the workflow eventually completes after several retries.
- **Ensure retries use the policy from Task 1:** Confirm that the RetryOptions configured in `AgentWorkflowImpl` (Task 1) apply to `RunAgentActivity` so that Temporal automatically retries the activity after each simulated failure.
- **Document how to trigger and observe retries:** In `docs/TEMPORAL_WALKTHROUGH.md` (created in Task 6), add a section describing:
  - How to trigger the failure scenario (e.g. send `{"message":"fail:Hello"}` to `POST /chat`).
  - What to expect in Temporal Web UI: multiple activity attempts with incrementing attempt numbers and the final success.
  - What to expect in Tempo: repeated worker activity spans/events with the same workflow ID and increasing attempt attributes (if surfaced), or at minimum multiple error events from the activity span.
  - How this behavior relates to the RetryOptions configured in code.
  </action>
  <verify>With the stack running, send POST /chat with a message starting with "fail:". In Temporal Web UI, open the workflow and confirm the RunAgent activity shows multiple attempts before succeeding. In Tempo, open the corresponding trace and confirm multiple error events or spans for the worker activity before a final successful attempt.</verify>
  <done>A controlled failure scenario exists that reliably triggers Temporal retries for RunAgentActivity, and documentation explains how to observe and reason about this behavior in Temporal Web and Tempo.</done>
</task>

<task type="auto">
  <name>Task 4: Expose workflow metadata (workflowId, task queue) to callers via the API</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/TemporalClientFactory.kt, api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt, api/src/main/kotlin/dev/otel/demo/api/models/Models.kt</files>
  <action>
- **Extend ChatResponse with optional workflow metadata fields:** In `Models.kt`, update `ChatResponse` to include optional workflow metadata while keeping existing clients compatible, e.g.:
  - `data class ChatResponse(val reply: String, val workflowId: String? = null, val taskQueue: String? = null)`.
- **Add a detailed workflow run helper in the client:** In `TemporalClientFactory.kt`:
  - Introduce a small data class (e.g. `data class AgentWorkflowRunResult(val reply: String, val workflowId: String, val taskQueue: String)`).
  - Add a new method `fun runAgentWorkflowDetailed(message: String): AgentWorkflowRunResult` that:
    - Creates the typed workflow stub with `WorkflowOptions` (including timeouts from Task 1).
    - Uses `WorkflowStub.fromTyped(workflow)` (import `io.temporal.client.WorkflowStub`) to access `workflowExecution.workflowId`.
    - Uses the task queue already chosen for options (env-configured or default) as the taskQueue value.
    - Calls `workflow.run(message)` to obtain the reply and returns an `AgentWorkflowRunResult` containing reply, workflowId, and taskQueue.
  - Update the existing `runAgentWorkflow(message: String): String` to delegate to `runAgentWorkflowDetailed(message).reply` so existing call sites remain valid.
- **Surface metadata in the chat route and logs:** In `ChatRoutes.chatRoutes`:
  - Replace the call to `client.runAgentWorkflow(body.message)` with `client.runAgentWorkflowDetailed(body.message)`.
  - Use the result to construct `ChatResponse(result.reply, result.workflowId, result.taskQueue)` and respond with it.
  - Enrich the existing structured log line to include the workflowId and taskQueue (e.g. via additional fields in the JSON log) so logs can be correlated with Temporal Web and Tempo.
  - Ensure spans already carry `workflow.id` and `task.queue` attributes from Phase 2; if any gaps remain, set them on the API span when metadata is available.
  </action>
  <verify>Send a POST /chat request and inspect the JSON response; confirm it includes reply plus non-null workflowId and taskQueue fields. Check API logs to see structured entries that include workflowId and taskQueue. In Temporal Web UI and Tempo, use the returned workflowId to locate the corresponding workflow execution and trace.</verify>
  <done>ChatResponse optionally exposes workflowId and taskQueue to callers while remaining backward compatible; API logs and spans include the same metadata so users can correlate API calls with Temporal Web and Grafana.</done>
</task>

<task type="auto">
  <name>Task 5: Add worker-focused tests using Temporal testing utilities</name>
  <files>worker/src/test/kotlin/dev/otel/demo/worker/AgentWorkflowImplTest.kt, worker/src/test/kotlin/dev/otel/demo/worker/RunAgentActivityTest.kt, worker/build.gradle.kts</files>
  <action>
- **Confirm temporal-testing dependency is available:** In `worker/build.gradle.kts`, verify that `io.temporal:temporal-testing:1.24.0` is present in the test dependencies. If not, add it under the testImplementation configuration.
- **Create a Temporal test for AgentWorkflow without real agent:** Add `AgentWorkflowImplTest` under `worker/src/test/kotlin/dev/otel/demo/worker/` that:
  - Uses `TestWorkflowEnvironment.newInstance()` to create an in-memory Temporal environment.
  - Creates a worker bound to a test-only task queue (e.g. `"test-agent-task-queue"`) and registers `AgentWorkflowImpl` as the workflow implementation.
  - Registers a lightweight test implementation of `RunAgentActivityInterface` (and the pre/post-processing interfaces) that does not perform HTTP calls; e.g. inline anonymous classes that return deterministic strings.
  - Starts the environment, obtains a workflow stub for `AgentWorkflow` via `environment.workflowClient.newWorkflowStub(...)`, and calls `run("Hello")`.
  - Asserts on the returned string to confirm the multi-step workflow composition works as expected (e.g. includes pre/post tags).
  - Closes the environment in a `finally` block or via try-with-resources style to avoid leaks.
- **Keep the existing RunAgentActivityTest but align with new behavior if needed:** If `RunAgentActivityTest` only asserts type membership, optionally extend it to cover the new constructor or basic behavior (without hitting the real agent), but do not duplicate the TestWorkflowEnvironment coverage from `AgentWorkflowImplTest`.
  </action>
  <verify>Run ./gradlew :worker:test and confirm the new Temporal test passes. Temporarily break the pre/post activity wiring (e.g. change the test stub) and confirm tests fail, then restore the correct behavior to ensure tests genuinely exercise the workflow.</verify>
  <done>Worker tests use Temporal's testing utilities to exercise AgentWorkflow end-to-end in-memory, without contacting a real Temporal cluster or the Python agent.</done>
</task>

<task type="auto">
  <name>Task 6: Document Temporal-centric troubleshooting and exploration paths</name>
  <files>docs/TEMPORAL_WALKTHROUGH.md, integration/README.md</files>
  <action>
- **Create a Temporal-focused walkthrough doc:** Add `docs/TEMPORAL_WALKTHROUGH.md` that complements `TRACE_WALKTHROUGH.md` and focuses on Temporal concepts:
  - Prerequisites: stack running (infra, agent, worker, API) and at least one successful `POST /chat`.
  - Section: "Inspect a simple workflow run" — how to open Temporal Web UI (from docker-compose defaults), filter by workflow type (AgentWorkflow), see inputs/outputs, and relate them to API requests.
  - Section: "See multi-step workflows" — describe how the pre-process, agent, and post-process activities appear in Temporal history; tie this back to `AgentWorkflowImpl` and the new activities from Task 2.
  - Section: "Observe retries and failures" — describe the `fail:` message trigger from Task 3, show how multiple activity attempts appear in Temporal Web and Tempo, and relate this to configured RetryOptions.
  - Section: "Identify stuck or long-running workflows" — explain how to look for workflows in running state, how the workflow run timeout applies, and how to reason about activity durations vs workflow duration.
  - Cross-link to `docs/TRACE_WALKTHROUGH.md` and `docs/BROKEN_OBSERVABILITY.md` so users can pivot between Temporal-centric and trace-centric debugging.
- **Link the Temporal walkthrough from integration docs:** In `integration/README.md`, add a short subsection or bullet (near the existing "View one trace in Grafana" section) that links to `docs/TEMPORAL_WALKTHROUGH.md` and describes it as the primary guide for inspecting workflows, multi-step compositions, and retries.
  </action>
  <verify>Open docs/TEMPORAL_WALKTHROUGH.md and confirm it covers basic workflow inspection, multi-step patterns, retries, and stuck/long-running workflows, with references to the concrete code. Open integration/README.md and confirm there is a working markdown link to the new doc from the integration/e2e section.</verify>
  <done>A dedicated Temporal walkthrough doc exists and is linked from integration/README; it explains how to inspect workflows, multi-step activity chains, failures, retries, and long-running behavior in Temporal Web and Tempo.</done>
</task>

</tasks>

<verification>
- Task 1: AgentWorkflow contract remains stable; workflow and activity timeouts and retry policies are explicitly configured in AgentWorkflowImpl and TemporalClientFactory.
- Task 2: AgentWorkflowImpl executes multiple activities (pre-process, agent call, post-process) in sequence; Temporal history and traces clearly show the multi-step pattern.
- Task 3: A `fail:`-prefixed message reliably triggers multiple RunAgentActivity attempts per workflow, with retries visible in Temporal Web and Tempo and documented in the Temporal walkthrough.
- Task 4: POST /chat responses and logs include workflowId and taskQueue, which can be used to locate the corresponding workflow and trace.
- Task 5: Worker tests using Temporal's testing utilities validate AgentWorkflow behavior in-memory without real Temporal or the agent.
- Task 6: docs/TEMPORAL_WALKTHROUGH.md and integration/README.md provide a Temporal-centric troubleshooting and exploration path aligned with the implemented patterns.
</verification>

<success_criteria>
- Phase 3 goal achieved: the demo showcases clear Temporal workflow patterns (timeouts, retries, multi-step activities) that are visible in both code and observability tooling, with supporting tests and documentation.
- An executor can implement Phase 3 using this plan without ambiguity, building on the foundations and observability from Phases 1 and 2.
</success_criteria>

<output>
After completion, create .planning/phases/03-temporal/03-01-SUMMARY.md summarizing what was implemented and any deviations.
</output>

