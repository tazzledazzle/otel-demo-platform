---
phase: 02-interview-ready-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - otel-demo-platform/docs/ARCHITECTURE.md
  - otel-demo-platform/docs/USE_CASES.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Architecture is documented: services, OTel, Temporal flow, and request path"
    - "Use cases and value of the demo are documented: why OTel, why Temporal, trace visibility"
  artifacts:
    - path: otel-demo-platform/docs/ARCHITECTURE.md
      provides: "Services, OTel flow, Temporal flow, and end-to-end request path"
      contains: "Components", "Data flow", "OTel", "Temporal", "request path"
    - path: otel-demo-platform/docs/USE_CASES.md
      provides: "Use cases and value (why OTel, why Temporal, trace visibility)"
      contains: "OpenTelemetry", "Temporal", "trace", "observability"
  key_links:
    - from: otel-demo-platform/docs/ARCHITECTURE.md
      to: "request path"
      via: "Explicit section or paragraph describing HTTP → API → workflow → worker → activity → agent → LangChain"
    - from: otel-demo-platform/docs/USE_CASES.md
      to: "value"
      via: "Explicit 'why OTel', 'why Temporal', and value of trace visibility"
---

<objective>
Bring ARCHITECTURE.md and USE_CASES.md to interview-ready: complete, consistent with PROJECT.md and Phase 2 success criteria, so an interviewer can explain services, OTel, Temporal flow, request path, and the use cases/value of the demo.

Purpose: Satisfy Phase 2 success criteria 1 and 2 (architecture documented; use cases and value documented).
Output: Updated ARCHITECTURE.md and USE_CASES.md in otel-demo-platform/docs/.
</objective>

<execution_context>
Execute plan per GSD workflow; produce SUMMARY in .planning/phases/02-interview-ready-docs/ after completion.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@otel-demo-platform/docs/ARCHITECTURE.md
@otel-demo-platform/docs/USE_CASES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete ARCHITECTURE.md for interview-ready</name>
  <files>otel-demo-platform/docs/ARCHITECTURE.md</files>
  <action>
- Keep existing Overview, Components table, Data flow, and Temporal mapping. Ensure they are accurate per current codebase (API/Worker/Agent, AgentWorkflow, RunAgent activity, POST /chat, POST /invoke).
- Add or refine a clear **Request path** (or equivalent) section: one paragraph or bullet list describing the end-to-end path: Client → POST /chat → API (starts workflow) → Temporal → Worker (picks workflow, runs RunAgent activity) → Activity HTTP to Agent POST /invoke → Agent (LangChain + Ollama) → response back. This is the path one trace follows.
- Add or refine **Observability (OTel)** section: (1) All services instrumented with OpenTelemetry; (2) W3C trace context propagated on HTTP (API↔client, activity↔agent) and via Temporal (API→worker); (3) OTLP (gRPC) export to collector (Grafana otel-lgtm); (4) one trace spans API, worker, agent, and LangChain steps in Tempo. No code snippets required; prose is enough.
- Do not duplicate README run instructions; reference README for "how to run". Cross-link to USE_CASES.md for "why we built this" (one sentence or link).
- Keep doc under docs/; no new files.
  </action>
  <verify>Grep ARCHITECTURE.md for "request path" or "Request path"; for "OTel" or "OpenTelemetry"; for "W3C" or "propagation"; for "OTLP" or "collector". All four concepts present.</verify>
  <done>Reader can understand what runs where, how a request flows, and how one trace is produced end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Complete USE_CASES.md for interview-ready</name>
  <files>otel-demo-platform/docs/USE_CASES.md</files>
  <action>
- Keep the four use cases (OTel/trace propagation, Temporal workflow+activities, LLM/agent observability, live-coding). Optionally add a one-line intro: "This demo supports a ~1hr interview: explain architecture, demo trace, implement one feature."
- Add or sharpen **Why OTel**: single trace across Kotlin API, Kotlin worker, and Python agent; vendor-neutral (Grafana otel-lgtm); W3C propagation so one request = one trace.
- Add or sharpen **Why Temporal**: durable workflow and activities; retries and history; same mental model as production (task queue, workflow runs).
- Add or sharpen **Value of trace visibility**: see one request across all services and LangChain steps in Grafana; debugging and demonstrating observability for agentic workflows.
- Keep doc concise (one page). Cross-link to ARCHITECTURE.md for "how it works" and to INTERVIEW_SCRIPT.md for "how to run the session" (one sentence or link each is enough).
- Do not add marketing fluff; stay technical and interview-focused.
  </action>
  <verify>Grep USE_CASES.md for "why" (or "Why") and "OTel" or "OpenTelemetry"; for "Temporal"; for "trace" and "visibility" or "observability". Cross-reference to ARCHITECTURE and INTERVIEW_SCRIPT present or added.</verify>
  <done>Reader understands why we use OTel, why Temporal, and the value of trace visibility for the demo.</done>
</task>

</tasks>

<verification>
- Grep both files for required concepts (request path, OTel, use cases, value).
- Read-through: ARCHITECTURE explains services + flow + OTel; USE_CASES explains why + value.
</verification>

<success_criteria>
1. ARCHITECTURE.md documents services, OTel (instrumentation, propagation, OTLP), Temporal flow, and request path.
2. USE_CASES.md documents use cases and value (why OTel, why Temporal, trace visibility).
3. Both docs consistent with PROJECT.md and cross-linked where appropriate.
</success_criteria>

<output>
After completion, create `.planning/phases/02-interview-ready-docs/02-01-SUMMARY.md`
</output>
