---
phase: 02-interview-ready-docs
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - otel-demo-platform/docs/INTERVIEW_SCRIPT.md
  - otel-demo-platform/docs/LIVE_FEATURE.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "An interview script exists with order of explain / demo / live-coding and rough timing for ~1hr"
    - "One live-coding feature is described: which pipeline step to add and where it fits"
  artifacts:
    - path: otel-demo-platform/docs/INTERVIEW_SCRIPT.md
      provides: "Interview script: order, timing, explain/demo/live-coding"
      contains: "min", "Architecture", "Grafana", "Live", "pipeline"
    - path: otel-demo-platform/docs/LIVE_FEATURE.md
      provides: "One live-coding feature: which step, where it fits"
      contains: "pipeline", "agent", "chain", "span"
  key_links:
    - from: otel-demo-platform/docs/INTERVIEW_SCRIPT.md
      to: otel-demo-platform/docs/ARCHITECTURE.md
      via: "Reference in script (e.g. 'Share docs/ARCHITECTURE.md or README')"
    - from: otel-demo-platform/docs/INTERVIEW_SCRIPT.md
      to: otel-demo-platform/docs/LIVE_FEATURE.md
      via: "Block 7 points to LIVE_FEATURE.md for step-by-step"
    - from: otel-demo-platform/docs/LIVE_FEATURE.md
      to: "agent code"
      via: "Explicit file path (e.g. agent/agent/chain.py) and where new step fits in pipeline"
---

<objective>
Bring INTERVIEW_SCRIPT.md and LIVE_FEATURE.md to interview-ready: script with clear order and ~1hr timing; one live-coding feature with which step to add and where it fits in the pipeline, so an interviewer can run the session and a candidate can implement one feature.

Purpose: Satisfy Phase 2 success criteria 3 and 4 (interview script; one live-coding feature described).
Output: Updated INTERVIEW_SCRIPT.md and LIVE_FEATURE.md in otel-demo-platform/docs/.
</objective>

<execution_context>
Execute plan per GSD workflow; produce SUMMARY in .planning/phases/02-interview-ready-docs/ after completion.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@otel-demo-platform/docs/ARCHITECTURE.md
@otel-demo-platform/docs/USE_CASES.md
@otel-demo-platform/docs/INTERVIEW_SCRIPT.md
@otel-demo-platform/docs/LIVE_FEATURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete INTERVIEW_SCRIPT.md for ~1hr flow</name>
  <files>otel-demo-platform/docs/INTERVIEW_SCRIPT.md</files>
  <action>
- Keep existing block table (Intro, Architecture, Use cases, Run stack, Grafana traces, Temporal optional, Live pipeline step, Buffer). Ensure total duration is ~60 min (adjust blocks by 1–2 min if needed).
- Make the **order** explicit: (1) Explain architecture, (2) Explain use cases, (3) Demo: run stack and one request, (4) Demo: show trace in Grafana (and optionally Temporal UI), (5) Live-coding: add one pipeline step per LIVE_FEATURE.md, (6) Buffer/Q&A. No reordering of explain-before-demo-before-live.
- In the Architecture block: add a reference to "docs/ARCHITECTURE.md (or README)" so the interviewer knows what to share. In the Use cases block: add reference to "docs/USE_CASES.md" if not already implied.
- In the Live block (e.g. block 7): state explicitly "Follow step-by-step in docs/LIVE_FEATURE.md" so script and live-feature doc are tied. Keep "~20 min" and "new pipeline step in Python agent".
- Add one line at top or bottom: "See docs/ARCHITECTURE.md, USE_CASES.md, LIVE_FEATURE.md for content to share." So all four DOC-01 aspects are linked from the script.
- Do not add runbook steps that duplicate README; script references "run the stack" and "one request" with pointer to README or integration/README for commands.
  </action>
  <verify>INTERVIEW_SCRIPT.md contains a table or list with durations summing to ~60; contains "Architecture", "Grafana" (or "trace"), "Live" (or "live-coding"); references LIVE_FEATURE.md and ARCHITECTURE or USE_CASES.</verify>
  <done>Interviewer can follow the script for explain → demo → live-coding in ~1hr with clear doc references.</done>
</task>

<task type="auto">
  <name>Task 2: Complete LIVE_FEATURE.md with which step and where it fits</name>
  <files>otel-demo-platform/docs/LIVE_FEATURE.md</files>
  <action>
- Keep Option A (new tool, e.g. get_weather) and Option B (new chain step, e.g. summarize). Ensure file paths are correct: agent code in `agent/` (e.g. `agent/agent/chain.py`, `agent/tests/test_chain.py`). If repo structure differs, use actual paths.
- Add a short **Where it fits** section (or equivalent): (1) Request path: API → Worker → RunAgent activity → Agent POST /invoke → LangChain pipeline (chain.py). (2) The new step is added inside the **Python agent** pipeline (chain.py): either a new tool in the tools list or a new runnable step in the chain (e.g. prompt | llm | new_step). (3) New span(s) appear in the same trace in Grafana under the agent service. This ties "one pipeline step" to ARCHITECTURE request path and to observability.
- For Option A and B, keep the existing step lists (add tool/step, wire, test, run, show in Grafana). Add one sentence each: "This fits in the pipeline as [tool invoked by executor | extra runnable after LLM]."
- Cross-link: "See ARCHITECTURE.md for the full request path" and "See INTERVIEW_SCRIPT.md block 7 for timing (~20 min)."
- Do not implement code; doc only. Ensure a candidate can read LIVE_FEATURE.md and know exactly which file to edit and where the new step sits in the pipeline.
  </action>
  <verify>Grep LIVE_FEATURE.md for "chain.py" or "agent/", "pipeline", "where it fits" or "Where it fits", "span", and reference to ARCHITECTURE or INTERVIEW_SCRIPT.</verify>
  <done>Reader knows which pipeline step to add (tool or chain step), in which file, and where it fits in the request path and trace.</done>
</task>

</tasks>

<verification>
- Grep INTERVIEW_SCRIPT for timing, doc refs, LIVE_FEATURE.
- Grep LIVE_FEATURE for pipeline position, file paths, ARCHITECTURE/INTERVIEW_SCRIPT refs.
- Read-through: script orders explain/demo/live and points to docs; live feature doc describes one step and its place in the pipeline.
</verification>

<success_criteria>
1. INTERVIEW_SCRIPT.md has order (explain / demo / live-coding) and rough timing ~1hr, with references to ARCHITECTURE, USE_CASES, LIVE_FEATURE.
2. LIVE_FEATURE.md describes one live-coding feature (e.g. new pipeline step) and where it fits (pipeline + file + trace).
3. Both docs consistent with ARCHITECTURE and PROJECT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/02-interview-ready-docs/02-02-SUMMARY.md`
</output>
