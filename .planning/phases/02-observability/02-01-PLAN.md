---
phase: 02-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/kotlin/dev/otel/demo/api/Telemetry.kt
  - api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt
  - api/src/main/kotlin/dev/otel/demo/api/Main.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/Telemetry.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/client/AgentClient.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt
  - worker/src/main/kotlin/dev/otel/demo/worker/Main.kt
  - agent/agent/telemetry.py
  - agent/agent/main.py
  - agent/agent/chain.py
  - docs/
  - integration/README.md
  - CONFIG.md
  - docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "One POST /chat produces one trace in Tempo with spans from API, worker, and agent"
    - "Spans carry domain attributes (message type, workflow ID, task queue) and key events (agent invocation start/end)"
    - "Trace-driven debugging is documented with TraceQL primary and UI fallback"
    - "Structured logs on the chat path include service name, trace/span IDs, and outcome"
    - "Latency and throughput metrics (chat count, latency histogram per service) appear in Grafana"
    - "One env var toggles broken observability; docs explain how to diagnose and fix"
  artifacts:
    - path: api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt
      provides: "Chat route with span and propagation"
    - path: worker/src/main/kotlin/dev/otel/demo/worker/client/AgentClient.kt
      provides: "Worker HTTP client with W3C context injection"
    - path: agent/agent/main.py
      provides: "Agent invoke with span events and structured log"
    - path: docs/
      provides: "Trace walkthrough (TraceQL + UI fallback), broken observability"
    - path: CONFIG.md
      provides: "OTEL env vars including broken-observability flag"
  key_links:
    - from: worker/client/AgentClient.kt
      to: "W3C traceparent/tracestate"
      via: "inject current Context into outgoing request headers"
    - from: api ChatRoutes.kt
      to: "Temporal workflow"
      via: "same trace context when starting workflow (if SDK supports)"
    - from: agent main.py
      to: "OTLP collector"
      via: "trace + metrics exporter"
---

<objective>
Phase 2 makes traces, logs, and a small metrics story (latency + throughput) a compelling tool that shows one request flowing API → Temporal → worker → agent → Grafana Tempo. Delivers: end-to-end trace propagation, enriched spans, TraceQL-first walkthrough docs, structured logging on the chat path, OTLP metrics in Grafana, and a config-flag broken-observability scenario with diagnosis docs.
</objective>

<execution_context>
Execute tasks in order 1–6. Each task has explicit files, action, verify, and done criteria. Honor locked decisions from 02-CONTEXT.md: TraceQL primary (UI fallback); metrics in scope; one env var for broken observability with one doc section for diagnose-and-fix.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-observability/02-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@integration/README.md
@CONFIG.md
@api/src/main/kotlin/dev/otel/demo/api/Telemetry.kt
@worker/src/main/kotlin/dev/otel/demo/worker/client/AgentClient.kt
@agent/agent/telemetry.py
@agent/agent/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix trace context propagation across all hops</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt, api/src/main/kotlin/dev/otel/demo/api/Main.kt, api/src/main/kotlin/dev/otel/demo/api/Telemetry.kt, worker/src/main/kotlin/dev/otel/demo/worker/client/AgentClient.kt, worker/src/main/kotlin/dev/otel/demo/worker/Telemetry.kt, agent/agent/telemetry.py, agent/agent/main.py</files>
  <action>
- **Propagation points:** (1) API (Ktor server) must create a root span for POST /chat and have W3C propagator set (already in Telemetry.kt). If Ktor does not auto-instrument, create a manual span in ChatRoutes.kt: start a span "POST /chat", run the handler in its scope, end span. Ensure the thread context is current when calling Temporal so the workflow runs in the same trace. (2) Worker: when calling the agent, the Ktor client in AgentClient.kt currently does NOT inject trace context. Fix: obtain the current OpenTelemetry context (io.opentelemetry.context.Context.current()), use the global propagator to inject into a carrier (e.g. a mutable map), then add traceparent and tracestate headers to the Ktor request in agentInvoke(). Use TextMapSetter to inject into HttpHeaders or request builder. (3) Agent: FastAPIInstrumentor already extracts W3C headers and creates child spans; ensure telemetry.py keeps set_global_textmap(TraceContextTextMapPropagator()) so incoming requests continue the trace.
- **API span:** If the project does not use opentelemetry-instrumentation-ktor-server, add a manual span in the POST /chat handler: tracer.spanBuilder("POST /chat").startSpan().use { span -> ... } and make the Temporal workflow call inside that scope so the workflow/activity can inherit context if the Temporal SDK propagates it; otherwise at least the API and worker→agent segments will be connected once worker injects.
- **Verify one trace:** After implementation, send one `curl -X POST http://localhost:8080/chat -H "Content-Type: application/json" -d '{"message":"Hi"}'`. In Grafana (localhost:3000) → Explore → Tempo, run TraceQL: `{ resource.service.name="otel-demo-api" }` (or `resource.service.name="otel-demo-api"`). Select a trace and confirm it contains spans from otel-demo-api, otel-demo-worker, and otel-demo-agent. Alternatively query by trace ID if the API response includes it (optional). Count: at least one span per service in the same trace.
  </action>
  <verify>One POST /chat; in Tempo TraceQL use resource.service.name="otel-demo-api", open a trace; confirm spans from api, worker, agent in one trace. ./gradlew :api:test :worker:test and agent pytest pass.</verify>
  <done>Single trace in Tempo for one chat request with spans from API, worker, and agent; worker Ktor client injects W3C context; agent receives and continues trace.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich spans with domain attributes and key events</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt, worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt, worker/src/main/kotlin/dev/otel/demo/worker/client/AgentClient.kt, agent/agent/main.py, agent/agent/chain.py</files>
  <action>
- **Attributes:** Add consistent domain attributes so traces are self-explanatory. API span: e.g. "message.type" = "chat" or "request.type" = "chat". Worker activity span: add "workflow.id" (from Temporal activity context if available via Activity.getExecutionContext().getInfo().getWorkflowId() or equivalent), "task.queue" (e.g. "agent-task-queue" or from context). Agent span: already has service name; add e.g. "message.type" = "invoke" if desired.
- **Events:** Add key events for agent invocation. Worker: before and after calling agentInvoke() add span events (e.g. "agent.invocation.start", "agent.invocation.end") with optional attributes (message length or no sensitive data). Agent: in the /invoke handler or in chain invocation, add span events "agent.invocation.start" and "agent.invocation.end" (or use the current span and addEvent) so both worker and agent show the boundary of the LLM call.
- Use OpenTelemetry API: span.setAttribute(key, value), span.addEvent(name). In Kotlin use the span from the current context; in Python use trace.get_current_span() and add_event.
  </action>
  <verify>Send POST /chat; in Tempo open the trace and inspect a worker and agent span; confirm attributes (e.g. workflow.id, task.queue) and events (agent.invocation.start/end) are present.</verify>
  <done>Spans carry domain attributes (message type, workflow ID, task queue) and key events (agent invocation start/end) where applicable.</done>
</task>

<task type="auto">
  <name>Task 3: Document trace-driven debugging walkthrough (TraceQL primary, UI fallback)</name>
  <files>docs/TRACE_WALKTHROUGH.md (new or existing), integration/README.md</files>
  <action>
- Create or extend a walkthrough doc (e.g. docs/TRACE_WALKTHROUGH.md) that leads with **TraceQL** and adds "Alternative: use the UI" where relevant. Content must include: (1) How to follow one request: e.g. send POST /chat, then in Grafana Explore → Tempo run TraceQL query `{ resource.service.name="otel-demo-api" }` (or equivalent), select the latest trace, describe that it shows API → worker → agent. (2) TraceQL examples: query by service name, by span name if useful, and how to find a trace by time range. (3) How to identify which service failed: e.g. look at span status/errors in the trace. (4) How to reason about latency: which span is slow (duration), parent-child relationship. (5) For each main step, add a short "Alternative: use the UI" (e.g. "Alternatively, open Explore → Tempo, select the service from the dropdown, and click Search to see traces in the UI").
- Ensure integration/README.md "View one trace in Grafana" section aligns with the walkthrough (TraceQL first, then UI alternative) and links to the full walkthrough doc if present.
- Per locked decision: TraceQL is primary; UI-click is fallback only.
  </action>
  <verify>Read docs/TRACE_WALKTHROUGH.md (or equivalent); confirm TraceQL examples and queries are first; confirm "Alternative: use the UI" appears where relevant; integration/README points to it.</verify>
  <done>Walkthrough doc exists with TraceQL examples and queries first; UI fallback documented; follow request, find failures, reason about latency.</done>
</task>

<task type="auto">
  <name>Task 4: Add structured logging on the main chat path</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt, worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt, worker/src/main/kotlin/dev/otel/demo/worker/client/AgentClient.kt, agent/agent/main.py</files>
  <action>
- Add **minimal structured logging** (service name, trace ID, span ID, high-level outcome) on the chat path so logs pair with traces during troubleshooting. Use a consistent format: e.g. JSON one line per log (`{"service":"otel-demo-api","trace_id":"...","span_id":"...","outcome":"ok"}`) or key=value; include trace_id and span_id from the current span context so log aggregation can correlate with Tempo.
- **API (ChatRoutes.kt):** On entry and on response (and on error), log one structured line with service name, trace_id, span_id (from Span.current().spanContext), outcome (ok / error). Replace or complement existing chatLog stderr with structured output.
- **Worker (RunAgentActivity or AgentClient):** Before and after agentInvoke, log one line with service name (otel-demo-worker), trace_id, span_id, outcome.
- **Agent (main.py invoke):** Before and after processing /invoke, log one line with service name (otel-demo-agent), trace_id, span_id (from trace.get_current_span().get_span_context()), outcome.
- Obtain trace_id/span_id: Kotlin use Span.current().spanContext.traceId/spanId; Python use trace.get_current_span().get_span_context().trace_id/span_id (format as hex). Do not add heavy logging libraries; keep minimal (e.g. println JSON or a single small util).
  </action>
  <verify>Send POST /chat; grep logs for trace_id and span_id; confirm same trace_id appears in API, worker, and agent logs; outcome present.</verify>
  <done>Structured logs on chat path in API, worker, agent with service name, trace_id, span_id, outcome.</done>
</task>

<task type="auto">
  <name>Task 5: Introduce small metrics story (latency + throughput) in Grafana</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/Telemetry.kt, api/src/main/kotlin/dev/otel/demo/api/routes/ChatRoutes.kt, worker/src/main/kotlin/dev/otel/demo/worker/Telemetry.kt, worker/src/main/kotlin/dev/otel/demo/worker/RunAgentActivity.kt, agent/agent/telemetry.py, agent/agent/main.py, docker-compose.yml, docs/ or integration/README.md</files>
  <action>
- **Metrics approach:** Use **OTLP metrics** to the same collector endpoint (http://localhost:4317). The otel-lgtm stack (Grafana) accepts OTLP gRPC on 4317; if the default image does not enable metrics ingestion, enable it via environment (e.g. otel-lgtm env to enable OTLP metrics) or document that metrics require an OTLP receiver with metrics enabled. Prefer reusing 4317 so one endpoint for traces and metrics; if otel-lgtm does not expose metrics in Explore, document "Metrics in Grafana" and how to add a dashboard or use Explore with the correct data source (Mimir or Prometheus if otel-lgtm forwards there).
- **Metrics to expose (keep small — latency + throughput only):** (1) **API:** chat request count (counter, e.g. chat.requests.total), chat request duration (histogram, e.g. chat.request.duration). Instrument in ChatRoutes when handling POST /chat. (2) **Worker:** activity invocation count (counter), activity duration (histogram). Instrument in RunAgentActivity around agentInvoke. (3) **Agent:** invoke count (counter), invoke duration (histogram). Instrument in main.py /invoke handler.
- **Implementation:** Kotlin: add MeterProvider to Telemetry.kt (OTLP metric exporter to same endpoint), create Meter from OpenTelemetry, in ChatRoutes/RunAgentActivity get LongCounter and DoubleHistogram, record on each request. Python: in telemetry.py add MeterProvider and OTLP metric exporter (same endpoint), create meter, in main.py /invoke get counter and histogram, record. Use semantic conventions where helpful (e.g. http.request.duration) or simple names (chat.request.duration).
- **Docs:** In the trace walkthrough or integration README, add a short "Metrics in Grafana" section: how to view latency and throughput (e.g. Explore → select Mimir/Prometheus data source if otel-lgtm provides it, or build a small dashboard with chat request rate and latency histogram per service). Show metrics alongside traces (e.g. "correlate by time range").
  </action>
  <verify>After POST /chat x2–3, open Grafana; confirm metrics for chat/request count and duration (or equivalent) are queryable; document where to find them. Builds and tests pass.</verify>
  <done>API, worker, and agent export latency (histogram) and throughput (counter) via OTLP; visible in Grafana; docs describe how to view them alongside traces.</done>
</task>

<task type="auto">
  <name>Task 6: Add config-flag broken observability and diagnose-and-fix doc</name>
  <files>api/src/main/kotlin/dev/otel/demo/api/Main.kt, api/src/main/kotlin/dev/otel/demo/api/Telemetry.kt, worker/src/main/kotlin/dev/otel/demo/worker/Main.kt, worker/src/main/kotlin/dev/otel/demo/worker/Telemetry.kt, agent/agent/telemetry.py, agent/agent/main.py, CONFIG.md, docs/BROKEN_OBSERVABILITY.md (new) or docs/TRACE_WALKTHROUGH.md</files>
  <action>
- **Env var:** Implement one **config flag** that deliberately misconfigures observability. Use a single env var, e.g. **OTEL_DISABLE_TRACING** (when set to "true" or "1", disable the trace exporter or do not register the span processor) or **OTEL_EXPORTER_OTLP_ENDPOINT** overwrite: when **OTEL_BROKEN_OBSERVABILITY=true** (or a dedicated name like **OTEL_DISABLE_TRACING**), either (a) set the OTLP endpoint to an invalid address (e.g. http://127.0.0.1:9999) so traces are sent but never received, or (b) disable the exporter so no spans are exported. Prefer (b) for "broken" so the interviewer can say "traces disappeared" and the candidate diagnoses. Recommendation: **OTEL_DISABLE_TRACING=true** — when set, API/Worker/Agent skip registering the OTLP span exporter (or use a no-op exporter), so traces never reach Tempo. Document the var in CONFIG.md.
- **Apply in all three services:** In API Main.kt / Telemetry.kt: if OTEL_DISABLE_TRACING is true, call initOpenTelemetry with a no-op exporter or do not add BatchSpanProcessor. Same for Worker and Agent. Keep one env var name consistent (e.g. OTEL_DISABLE_TRACING).
- **Document "how to diagnose and fix":** Add a dedicated subsection or new doc (e.g. **docs/BROKEN_OBSERVABILITY.md**) for the interview script: (1) Scenario: "Traces stopped showing up in Grafana." (2) How to diagnose: check that services are running, check OTEL_EXPORTER_OTLP_ENDPOINT and collector reachable; check for OTEL_DISABLE_TRACING (or the chosen var) set to true in the environment. (3) How to fix: unset OTEL_DISABLE_TRACING or set it to false and restart the service(s). (4) Verify: send POST /chat and confirm trace appears in Tempo. Link this doc from CONFIG.md in the observability section.
- **CONFIG.md:** Add a row for the new env var (e.g. OTEL_DISABLE_TRACING) under API, Worker, and Agent (or one shared "Observability" section): "When set to true/1, disables trace export (for interview: broken observability scenario). Default: unset (traces enabled)."
  </action>
  <verify>Set OTEL_DISABLE_TRACING=true for one service, restart, send POST /chat; confirm that service's spans do not appear in Tempo (or no trace). Unset, restart; confirm trace reappears. CONFIG.md and BROKEN_OBSERVABILITY.md (or equivalent) describe the flag and diagnose-and-fix steps.</verify>
  <done>One env var (e.g. OTEL_DISABLE_TRACING) toggles broken observability; all three services respect it; CONFIG.md and diagnose-and-fix doc exist for the interview.</done>
</task>

</tasks>

<verification>
- Task 1: One trace in Tempo for one POST /chat with api, worker, agent spans; worker injects W3C context into agent call.
- Task 2: Spans show domain attributes and agent invocation events.
- Task 3: Walkthrough doc leads with TraceQL; UI fallback noted.
- Task 4: Structured logs with trace_id, span_id, outcome on chat path.
- Task 5: Metrics (count + histogram) per service in Grafana; docs mention how to view.
- Task 6: OTEL_DISABLE_TRACING (or chosen name) implemented and documented; diagnose-and-fix doc for interview.
</verification>

<success_criteria>
- Phase 2 goal achieved: traces, logs, and small metrics story are a compelling tool showing one request API → Temporal → worker → agent → Grafana; TraceQL-primary docs; broken observability flag and fix doc.
- An executor can implement Phase 2 using this plan without ambiguity.
</success_criteria>

<output>
After completion, create .planning/phases/02-observability/02-01-SUMMARY.md summarizing what was implemented and any deviations.
</output>
