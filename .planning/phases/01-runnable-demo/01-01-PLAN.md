---
phase: 01-runnable-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - otel-demo-platform/README.md
  - otel-demo-platform/integration/README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Reviewer can start all infra (Temporal, Grafana otel-lgtm) from documented steps"
    - "Reviewer can start API, worker, and agent and confirm they are healthy via health endpoints"
    - "Reviewer can send a chat request and receive a response using documented test data or example"
    - "Reviewer can view one end-to-end trace in Grafana spanning API, worker, and agent"
  artifacts:
    - path: otel-demo-platform/README.md
      provides: "Primary entry with prerequisites, start order, smoke check, curl, Grafana steps"
      contains: "docker compose", "/health", "POST /chat", "Explore", "Tempo"
    - path: otel-demo-platform/integration/README.md
      provides: "Integration/E2E steps consistent with README"
      contains: "docker compose", "health", "sample_requests", "Tempo"
  key_links:
    - from: otel-demo-platform/README.md
      to: otel-demo-platform/integration/README.md
      via: "cross-link (e.g. 'See integration/README.md for E2E steps')"
    - from: otel-demo-platform/README.md
      to: otel-demo-platform/test-data/sample_requests.json
      via: "At least one full curl and pointer to test-data/"
    - from: otel-demo-platform/integration/README.md
      to: otel-demo-platform/test-data/sample_requests.json
      via: "Example curl and pointer to test-data/"
---

<objective>
Make the root README and integration README complete and consistent so a reviewer can run the full demo from scratch: start infra, start services with documented health checks, send a chat request (with test data pointer), and view one trace in Grafana.

Purpose: Satisfy DOC-02 and Phase 1 success criteria (four reviewer-verifiable outcomes).
Output: Updated README.md and integration/README.md; no code changes.
</objective>

<execution_context>
@/Users/terenceschumacher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runnable-demo/01-CONTEXT.md

# Existing docs (current shape)
@otel-demo-platform/README.md
@otel-demo-platform/integration/README.md
@otel-demo-platform/test-data/sample_requests.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete root README for runnable demo</name>
  <files>otel-demo-platform/README.md</files>
  <action>
- Keep Prerequisites as-is: Ollama (link to ollama.ai), pull one model (e.g. llama3.2); JDK 17+, Python 3.11+, Docker, Docker Compose. No OS-specific install steps.
- Set start order explicitly: (1) Infra — `docker compose up -d`; (2) Agent — `cd agent && pip install -r requirements.txt && python -m agent.main` (Ollama + model required); (3) Worker — `cd worker && ./gradlew run`; (4) API — `cd api && ./gradlew run`.
- Add smoke check: after starting each service (or all four), document hitting health endpoints so reviewer can confirm before sending chat. Include: API `GET http://localhost:8080/health`, Agent `GET http://localhost:8000/health` (default AGENT_PORT). Use exact URLs. Optional: one-line "Quick validation" section listing both health URLs.
- Keep one full curl example for POST /chat; use a body from test-data (e.g. `{"message":"Hello"}`). Add a sentence pointing to `test-data/sample_requests.json` for more examples (e.g. "What is 2 + 2?").
- Add Grafana step-by-step: open Grafana at http://localhost:3000 (admin/admin); go to Explore → Tempo; how to find the trace (e.g. query by service name such as api or otel-demo-api, or by time range); expect one trace with spans for API, worker, and agent. No custom dashboard this phase.
- Add cross-link to integration/README.md for full E2E/integration steps (e.g. in Quick Start or Project Layout).
- Do not add a runbook under .planning/; keep everything in the app repo.
  </action>
  <verify>README.md contains the strings "docker compose", "/health", "8080", "8000", "POST /chat", "Explore", "Tempo", "test-data" or "sample_requests", and a reference to integration/README.md.</verify>
  <done>Reviewer can follow README to start infra and services, confirm health, send one curl, and open Grafana to find one trace.</done>
</task>

<task type="auto">
  <name>Task 2: Complete integration/README.md and align with root README</name>
  <files>otel-demo-platform/integration/README.md</files>
  <action>
- Keep start order: Infra → Agent → Worker → API (match root README).
- Add smoke check: document hitting GET http://localhost:8080/health (API) and GET http://localhost:8000/health (Agent) so reviewer can confirm each piece before sending chat.
- Include one full curl for POST /chat (e.g. from sample_requests.json) and point to `test-data/sample_requests.json` for more examples.
- Add short Grafana steps: open http://localhost:3000, Explore → Tempo, how to find the trace (service name or time range), expect one trace with API, worker, and agent spans.
- Add cross-link to main README (e.g. "See root README for prerequisites and architecture").
- Wording and section titles may differ slightly; ensure consistency on start order, health URLs, test data location, and Grafana expectations (one trace, API + worker + agent spans).
  </action>
  <verify>integration/README.md contains "docker compose", "health", "8080", "8000", "sample_requests" or "test-data", "Tempo", and reference to main README or root README.</verify>
  <done>Integration doc is complete and consistent with root README; reviewer can run E2E using either doc and get the same outcome.</done>
</task>

</tasks>

<verification>
- Grep README.md and integration/README.md for required strings (health URLs, Tempo, test-data).
- Manually: follow documented steps in order; hit both health endpoints; send one POST /chat; open Grafana → Explore → Tempo and confirm one trace with API, worker, agent spans.
</verification>

<success_criteria>
1. All infra start steps documented (docker compose).
2. Start order and health checks documented (API and agent GET /health).
3. At least one full curl and pointer to test-data/sample_requests.json in both docs.
4. Grafana steps: open Grafana, Explore → Tempo, how to find trace, expect one trace with API/worker/agent spans.
5. README and integration/README cross-linked and consistent.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runnable-demo/01-01-SUMMARY.md`
</output>
